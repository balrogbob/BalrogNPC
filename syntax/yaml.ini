[Syntax]
name = YAML
detect.ext = .yml,.yaml
detect.regex = (?m)^\s*-\s|(?m)^[A-Za-z0-9_.-]+\s*:\s|(?m)^\s*---\s
detect.contains = Header:, Footer:
detect.priority = 30
# Tag colors
tag.number.fg = #00688B
tag.number.bg =
tag.keyword.fg = #00008B
tag.keyword.bg =
tag.string.fg = #A31515
tag.string.bg =
tag.def.fg = #006400
tag.def.bg =
tag.variable.fg = #003366
tag.variable.bg =
tag.comment.fg = #666666
tag.comment.bg =
tag.constant.fg = #800080
tag.constant.bg =
tag.builtin.fg = #0066CC
tag.builtin.bg =
tag.decorator.fg = #00688B
tag.decorator.bg =
tag.todo.fg = #000000
tag.todo.bg = #FFF2A6
keywords.csv = true,false,yes,no,on,off,null,~,inherit
builtins.csv = ---,...,&,*,>,|,!!,!?
regex.STRING_RE = ("(?:\\.|[^"\\])*")|('(?:[^']|'')*')|(\|[^\n]*\n(?:[ \t]+.*\n)+)|(>[^\n]*\n(?:[ \t]+.*\n)+)|(?m)^\s*data:\s*(\S.*)$
regex.COMMENT_RE = #[^\n]*
regex.DECORATOR_RE = (?m)(?:^|\s)(?:[&*]|!{1,2})([A-Za-z_][\w:-]*)|(?m)^%%(YAML|TAG)\b
regex.NUMBER_RE = \b(?:0[xX][0-9A-Fa-f]+|-?(?:0|[1-9]\d*)(?:\.\d+)?(?:[eE][+-]?\d+)?)\b
regex.DUNDER_RE = (?m)^[ \t]*(?:-\s+)?key:\s*(\".*?\"|'.*?'|\S.*)$
regex.VAR_ASSIGN_RE = (?m)^[ \t]*(?:-\s+)?value:\s*(\".*?\"|'.*?'|\S+)
regex.CONSTANT_RE = (?m)^[ \t]*(?:-\s+)?([A-Z][_A-Z0-9]+)\s*:
regex.ATTRIBUTE_RE = (?m)^[ \t]*(?:-\s+)?([A-Za-z_][A-Za-z0-9_.-]*)(?=\s*:)
regex.TODO_RE = #[^\n]*\b(TODO|FIXME|NOTE)\b
regex.FSTRING_RE = \b\b
regex.VAR_ANNOT_RE = \b\b
regex.CLASS_RE = \b\b
regex.CLASS_BASES_RE = \b\b
regex.SELFS_RE = \b\b